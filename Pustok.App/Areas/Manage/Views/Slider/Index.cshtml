@model List<Slider>

<div class="container">
    <div class="row mb-3">
        <div class="col">
            <h2>Slider List</h2>
        </div>
        <div class="col text-end">
            <a asp-area="Manage" asp-controller="Slider" asp-action="Create" class="btn btn-success">Create New Slider</a>
        </div>
    </div>
    <div class="row">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Image</th>
                    <th scope="col">Title</th>
                    <th scope="col">Desccription</th>
                    <th scope="col">Button Text</th>
                    <th scope="col">Button Link</th>
                    <th scope="col">Order</th>
                    <th scope="col">Handle</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <img width="100" height="100" src="~/assets/image/bg-images/@item.ImageUrl" alt="Alternate Text" />
                        </td>
                        <td>@item.Title</td>
                        <td>@item.Description</td>
                        <td>@item.ButtonText</td>
                        <td>@item.ButtonLink</td>
                        <td>@item.Order</td>
                        <td>
                            <button class="btn btn-sm btn-primary detail-btn" data-id="@item.Id">Details</button>
                            <a asp-area="Manage" asp-controller="Slider" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-info">Edit</a>
                            <a asp-area="Manage" asp-controller="Slider" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger delete">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Detail Modal -->
<div class="modal fade" id="detailModal" tabindex="-1" role="dialog" aria-labelledby="detailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <!-- Modal content will be loaded here -->
        </div>
    </div>
</div>

@section Scripts
{
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        $(document).ready(function(){
            // Delete functionality
            $(".delete").click(function(e){
                e.preventDefault();
                let url = $(this).attr("href");
                Swal.fire({
                    title: "Are you sure?",
                    showCancelButton: true,
                    confirmButtonText: "Delete"
                }).then((result) => {
                    if (result.isConfirmed) {
                        fetch(url).then(res=>{
                            if(res.ok) {
                                Swal.fire({
                                    title: "Deleted!",
                                    text: "Your file has been deleted.",
                                    icon: "success"
                                });
                                window.location.reload();
                            }
                            else{
                                Swal.fire({
                                    title: "Error!",
                                    text: "Something went wrong.",
                                    icon: "error"
                                });
                            }
                        });
                    }
                });
            });

            // Detail Modal functionality
            $(".detail-btn").click(function() {
                let id = $(this).data('id');
                let url = `/manage/slider/detail/${id}`;

                fetch(url)
                    .then(response => response.text())
                    .then(html => {
                        $("#detailModal .modal-content").html(html);
                        $("#detailModal").modal('show');
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        Swal.fire({
                            title: "Error!",
                            text: "Failed to load genre details.",
                            icon: "error"
                        });
                    });
            });
        });
    </script>
}